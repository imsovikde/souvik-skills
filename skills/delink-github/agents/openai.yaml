interface:
  display_name: "Delink GitHub"
  short_description: "Isolate copied repositories and start clean Git history"
  default_prompt: "Use $delink-github to safely detach, rename, clean inherited metadata, purge GitHub linkage, and start clean local Git history for this repository."
